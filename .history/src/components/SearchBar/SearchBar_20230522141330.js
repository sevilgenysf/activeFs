import React from 'react';
import {TextInput, Text, View} from 'react-native';

const SearchBar = () => {
  return (
    <View>
      <Text>Merhaba</Text>;
    </View>
  );
};

export default SearchBar;
